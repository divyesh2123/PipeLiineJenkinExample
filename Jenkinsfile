 pipeline {
    agent {
        label 'built-in'
    }

    stages {
        stage('Test') {
            steps {
                echo 'Jenkins Declarative Pipeline is working'
            }
        }
    }
}